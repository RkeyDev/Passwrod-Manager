from gui.application_window import AppWindow


if __name__ == "__main__":
    #The starting point of the program.
    #It will open the GUI and start the application.

    main_window = AppWindow()    
    main_window.mainloop()
